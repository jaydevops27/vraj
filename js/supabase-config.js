// ---------------------------------------------------------------------------
// Fill these in with values from your Supabase project
// (Project Settings -> API). The "anon" key is safe to expose in client-side
// code — it only grants whatever Row Level Security policies allow
// (see sql/schema.sql: public can INSERT into rsvps and SELECT lantern_wall,
// nothing else).
// ---------------------------------------------------------------------------
export const SUPABASE_URL = 'https://YOUR-PROJECT-REF.supabase.co';
export const SUPABASE_ANON_KEY = 'YOUR-ANON-PUBLIC-KEY';
